<h1>Products (<%= @products.length %>)</h1>
<%= link_to "New Product", "/products/new" %>

<% @products.each do |product| %>
  <p>
  	<%= product[:id] %>, <%= product[:name] %>, <%= product[:description]%>, <%= product[:price_in_cents] %>
  </p>

<% end %>